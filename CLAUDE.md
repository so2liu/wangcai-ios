# Project instructions

## iOS device deployment

- After every verified, runnable iOS update, install the latest build on both paired devices: `iPhone` and `OMG`.
- Installing on only one of the two devices is incomplete deployment.
- Do not automatically launch or otherwise control the app on either phone after installation unless the user explicitly asks.
